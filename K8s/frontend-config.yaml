apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: fixmate-frontend-config
  namespace: fixmate
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT