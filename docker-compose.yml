services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: fixmatedb
      POSTGRES_USER: fixmate
      POSTGRES_PASSWORD: fixmate
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
